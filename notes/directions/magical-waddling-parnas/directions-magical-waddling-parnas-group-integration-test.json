{
  "meta": {
    "title": "Add Mutual Exclusion Validation to CellDocument Builder",
    "source_branch": "magical-waddling-parnas"
  },
  "architecture_notes": [
    "Changes are within the castep-cell-io crate. Primary changes in cell_document.rs, plus additive pub use re-exports in 4 mod.rs files (one line each, no logic change). No changes to castep_cell_fmt, lib.rs exports, or README.",
    "Use bon's Custom Finishing Function pattern (finish_fn) rather than a separate .validate() method. This bakes validation into build() so callers cannot forget to validate.",
    "The bon derive on CellDocument renames the generated infallible method to build_internal() (private). A manual impl<S: IsComplete> CellDocumentBuilder<S> provides a public build() -> CResult<CellDocument> that calls build_internal() then runs 4 validation rules.",
    "Validation rules check mutual exclusion of at-most-one-Of-N optional fields. The counting idiom uses .is_some() on each field, collected into an array, .iter().filter(|&&x| x).count(), and checks count > 1.",
    "kpoints_mp_offset and spectral_kpoints_mp_offset are NOT mutually exclusive with anything \u2014 they are tuning parameters that can coexist with other kpoint specifications.",
    "D2 absorption: bs_kpoint_path and bs_kpoints_list are backward-compat aliases for spectral_kpoint_path and spectral_kpoints_list. They are included in the ^4 mutual-exclusion check (at most 1 of 6), and BS_ parsing in from_cell_file is guarded to prevent double-population.",
    "The #[allow(clippy::duplicated_attributes)] on line 237 must be preserved \u2014 the derive(Builder) + #[builder(...)] combination intentionally triggers this clippy lint.",
    "ParamDocument uses a separate .validate() -> Result<Self, String> pattern. This plan deliberately diverges for CellDocument \u2014 the fallible builder approach is chosen for one-shot builder usage where the caller cannot forget validation.",
    "Library code in this phase follows ch12-04 TDD: tests claim interfaces first via tdd_interface, then implementations evolve to meet them. See the tdd-pattern.md reference (resolved path available at /Users/tony/programming/rust-development-pipeline/skills/elaborate-directions/references/tdd-pattern.md)."
  ],
  "known_pitfalls": [
    "PITFALL-1 (IsComplete trait visibility): cell_document_builder::IsComplete is a private module generated by the derive macro on CellDocument. The manual impl<S: cell_document_builder::IsComplete> block MUST be placed in the same parent module (cell_document) as the derive, i.e., in the same file (cell_document.rs). If placed elsewhere, the path will not resolve. Compile check 'cargo check -p castep_cell_io' must pass before proceeding.",
    "PITFALL-2 (maybe_ setter naming): For Option<T> fields, bon generates setters prefixed with maybe_ (e.g., maybe_kpoints_list). Do NOT use the non-prefixed name (e.g., .kpoints_list()) \u2014 the compiler will error. If 'cargo build' succeeds, the correct setters are used.",
    "PITFALL-3 (Missing optional field in from_cell_file builder): When converting the struct literal to builder calls, if a maybe_* setter is omitted, the field silently becomes None (no compile error). Cross-reference every field in the struct literal against the builder chain. The elaboration lists all 48 optional fields in order \u2014 check each one off.",
    "PITFALL-4 (bs_kpoint_path_spacing is NOT mutually exclusive): bs_kpoint_path_spacing is a tuning parameter, not a kpoint specification. Do NOT include it in the ^4 validation check. It is already correctly excluded from the plan.",
    "PITFALL-5 (Test fixture entry types): The test module needs entry-level types (PositionFracEntry, BsKpointPathEntry, PhononKpointPathEntry, PhononKpointListEntry) that are currently not re-exported from their parent modules. TASK-1 adds pub use re-exports to 4 mod.rs files. Verify these are added before attempting to compile tests.",
    "PITFALL-6 (bon v3.9.1 version pin): The finish_fn attribute and IsComplete trait naming depend on bon v3.9.1 behavior. The version is pinned in Cargo.toml. If bon is upgraded later, run 'cargo check -p castep_cell_io' immediately to catch breaking changes.",
    "PITFALL-7 (BS_ parse ordering): The bs_kpoint_path/is_some() guard depends on spectral_kpoint_path being parsed first (lines 529-543 come before lines 545-553). This ordering already exists in the current code. Do NOT reorder these parsing blocks.",
    "PITFALL-8 (existing test preservation): The single existing test test_parse_mg2sio4_cell is #[ignore] with an empty input string. New tests should be added to the same #[cfg(test)] mod tests block without modifying or removing the existing test.",
    "PITFALL-9 (import paths for tests): The test module imports entry types from public re-export paths. Do NOT import from private module paths (e.g., crate::cell::positions::positions_frac::PositionFracEntry). Use the public re-export paths from parent mod.rs files."
  ],
  "task_groups": [
    {
      "group_id": "group-integration-test",
      "reason": "Adds an integration test that exercises the full parse\u2192build pipeline with conflicting k-point blocks. Depends on both the fallible builder and from_cell_file updates being in place. Creates a new file in tests/.",
      "tasks": [
        "TASK-3"
      ],
      "depends_on_groups": [
        "group-from-cell-file"
      ]
    }
  ],
  "tasks": [
    {
      "id": "TASK-3",
      "kind": "direct",
      "description": "Add integration test that parses a .cell string containing conflicting KPOINT_LIST and KPOINT_MP_GRID blocks and verifies the parse returns Err.",
      "files_in_scope": [
        "castep_cell_io/tests/mutual_exclusion_validation.rs"
      ],
      "changes": [
        {
          "path": "castep_cell_io/tests/mutual_exclusion_validation.rs",
          "action": "create",
          "guidance": "Create a new integration test file. The test function should: (1) define a &str input containing a minimal valid .cell file with %BLOCK LATTICE_CART, %BLOCK POSITIONS_FRAC, %BLOCK KPOINTS_LIST, and the keyword KPOINT_MP_GRID : 2 2 2 \u2014 so both kpoint-list and kpoint-mp-grid are specified, which violates rule ^3. (2) call castep_cell_fmt::parse::<CellDocument>(input) and assert the result is Err. Import castep_cell_fmt::parse and castep_cell_io::CellDocument. The test function name should describe the scenario, e.g., parse_rejects_conflicting_kpoint_specs."
        }
      ],
      "wiring_checklist": [
        {
          "kind": "fn_call",
          "file": "castep_cell_io/tests/mutual_exclusion_validation.rs",
          "detail": "Integration test file compiles and is discovered by cargo test. Verify the test appears in 'cargo test --list' output."
        }
      ],
      "type_reference": {
        "parse": "pub fn parse<T: FromCellFile>(input: &str) -> CResult<T>"
      },
      "acceptance": [
        "cargo check -p castep_cell_io",
        "cargo test -p castep_cell_io --test mutual_exclusion_validation",
        "cargo test -p castep_cell_io",
        "cargo clippy"
      ],
      "depends_on": [
        "TASK-2"
      ]
    }
  ]
}
